export const Wisdom = ({ text }) => {
  return (
    <div className="wisdom-container glass-panel">
      <h2 className="glass-panel-title">WISDOM</h2>
      {/* <p className="wisdom-quote">{text}</p> */}
      <p className="wisdom-quote">"The only way out is through."</p>
    </div>
  );
};
